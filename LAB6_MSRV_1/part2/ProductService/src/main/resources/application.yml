server:
  port: 8901

spring:
  application:
    name: product-service
  cloud:
    consul:
      host: localhost           # o el host/servicio 'consul' si estás en Docker Compose
      port: 8500
      discovery:
        register: true
        prefer-ip-address: true        # evita hostnames no resolubles
        instance-id: ${spring.application.name}-${server.port}
        health-check-url: http://host.docker.internal:8901/actuator/health
        health-check-interval: 10s
        deregister-critical-service-after: 1m
        # Si corres en Docker Compose: usa el nombre del servicio/host correcto
        # hostname: product-service
        # scheme: http
management:
  endpoints:
    web:
      exposure:
        include: health,info,readiness,liveness
  endpoint:
    health:
      probes:
        enabled: true
